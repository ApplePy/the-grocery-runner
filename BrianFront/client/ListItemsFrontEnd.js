/**
 * Created by darryl on 2015-11-14.
 */

/*Template.ListItems.helpers ({
    ShowListItems: function() {
        var result = [];
        var db = GroceryList.find({_id: {$e: Session.get("ListID")}}, {sort: {name: 1}});

        for (var entry in db) {
            result.concat(db[entry]);
        }

        return result;
    }
});*/